xml
<buvi>
    <model id="BankManagement.ResPartnerBank" name="ResPartnerBank" description="Partner Bank">
        <field name="AccountNumber" type="string" label="Account Number" required="true" />
        <field name="AccountType" type="string" label="Account Type" />
        <!-- Add other fields from the res.partner.bank model if needed -->
    </model>
</buvi>
