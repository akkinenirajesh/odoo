C#
public partial class DecimalPrecision 
{
    public virtual void LoadPosDataFields(int configId)
    {
        // Implementation of _load_pos_data_fields method
        // Access the env object to interact with other models and data.
        // Example:
        // var fields = Env.Model("DecimalPrecision").Search(new List<object> { configId }, new List<string> { "Id", "Name", "Digits" }); 
    }
}
